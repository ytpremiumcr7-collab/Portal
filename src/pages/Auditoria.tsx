import { useState } from "react"; import { trpc } from "@/providers/trpc"; import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"; import { Button } from "@/components/ui/button";
export default function Auditoria(){const [page,setPage]=useState(1);const q=trpc.auditoria.list.useQuery({page,pageSize:25});return <div className="space-y-6"><h2 className="text-2xl font-bold text-white">Bitácora de trazabilidad</h2><Card className="border-slate-700 bg-slate-800/50"><CardHeader><CardTitle className="text-white">Cambios registrados</CardTitle></CardHeader><CardContent className="overflow-x-auto"><table className="w-full"><thead><tr className="border-b border-slate-700"><th className="p-3 text-left text-xs text-slate-400">Fecha</th><th className="p-3 text-left text-xs text-slate-400">Usuario</th><th className="p-3 text-left text-xs text-slate-400">Acción</th><th className="p-3 text-left text-xs text-slate-400">Entidad</th><th className="p-3 text-left text-xs text-slate-400">Motivo</th></tr></thead><tbody>{q.data?.items.map(a=><tr key={a.id} className="border-b border-slate-800"><td className="p-3 text-xs text-slate-300">{new Date(a.timestamp).toLocaleString("es-MX")}</td><td className="p-3 text-sm text-white">{a.actor?.name}</td><td className="p-3 text-xs text-amber-300">{a.accion}</td><td className="p-3 text-xs text-slate-300">{a.entidad}#{a.entidadId}</td><td className="p-3 text-xs text-slate-400">{a.motivo||"—"}</td></tr>)}</tbody></table></CardContent></Card><div className="flex justify-end gap-2"><Button variant="outline" disabled={page<=1} onClick={()=>setPage(p=>p-1)}>Anterior</Button><span className="self-center text-sm text-slate-400">Página {page} de {q.data?.pageCount??1}</span><Button variant="outline" disabled={!q.data||page>=q.data.pageCount} onClick={()=>setPage(p=>p+1)}>Siguiente</Button></div></div>}
